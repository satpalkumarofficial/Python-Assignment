
# ? Q2. Python program to find the area of a triangle whose sides are given.
# A = 1/2 × b × h.
# b, h = 20, 30

print(f"Area of the triangle: {1/2 * float(input('Enter base of triangle: ')) * float(input('Enter height of triangle: '))}")
